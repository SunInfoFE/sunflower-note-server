const config = {
    // 启动端口号
    port: 3000,
    // 数据库配置
    database: {
        DATABASE: 'myblog_db',
        USERNAME: 'root',
        PASSWORD: 'hg1995',
        PORT: 3306,
        HOST: 'localhost'
    }
}

module.exports = config