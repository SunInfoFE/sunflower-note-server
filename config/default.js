const config = {
    // 启动端口号
    port: 3000,
    // 数据库配置
    database: {
        DATABASE: 'weeklyreport_db',
        USERNAME: 'admin',
        PASSWORD: 'admin',
        PORT: 33060,
        HOST: '192.168.212.50'
    }
}

module.exports = config