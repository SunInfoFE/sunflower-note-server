const config = {
    // 启动端口号
    port: 3000,
    // 数据库配置
    database: {
        DATABASE: 'weeklyreport_db',
        USERNAME: 'root',
        PASSWORD: '123',
        PORT: 3306,
        HOST: 'localhost'
    }
}

module.exports = config